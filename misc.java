if( str.equalsIgnoreCase("c#") )
		{
			label = new Label("C#");
		}
		
		if( str.equalsIgnoreCase("d") )
		{
			label = new Label("d");
		}
		
		if( str.equalsIgnoreCase("d#") )
		{
			label = new Label("d#");
		}
		
		if( str.equalsIgnoreCase("e") )
		{
			label = new Label("e");
		}
		
		if( str.equalsIgnoreCase("f") )
		{
			label = new Label("f");
		}
		if( str.equalsIgnoreCase("f#") )
		{
			label = new Label("f#");
		}
		
		if( str.equalsIgnoreCase("g") )
		{
			label = new Label("g");
		}
		
		if( str.equalsIgnoreCase("g#") )
		{
			label = new Label("g#");
		}
		
		if( str.equalsIgnoreCase("a") )
		{
			label = new Label("a");
		}
		
		if( str.equalsIgnoreCase("a#") )
		{
			label = new Label("a#");
		}
		
		if( str.equalsIgnoreCase("b") )
		{
			label = new Label("b");
		}

    // changed into switch statement